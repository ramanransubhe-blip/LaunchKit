# Code Quality Standards

LaunchKit enforces strict type safety and formats:

- **ESLint**: Standard JS/TS rules with `unused-imports` validation.
- **Prettier**: Strict layout formats check.
- **Verification Gates**: Pull requests require clean runs of `pnpm verify` before merging.
