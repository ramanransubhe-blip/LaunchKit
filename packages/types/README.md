# @devlaunchkit/types

Compatibility package for shared TypeScript types & data contracts.

## Structure

- `src/`: Main source files.

## Auth Types

The auth surface is re-exported from `@devlaunchkit/auth` so the auth package
remains the canonical source of truth.
