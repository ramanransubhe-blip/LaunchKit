# LaunchKit Examples

This directory hosts standalone snippets, config samples, and templates showing integrations and implementations of LaunchKit features.

## Examples Listing

- `docker/`: Setup variants for production deployment.
- `auth-custom/`: Integration templates for other auth providers.
- `database-seeding/`: Advanced mock databases.
